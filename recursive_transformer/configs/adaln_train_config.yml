seed: 42
num_epochs: 40
accum_steps: 32
max_numsteps: 12
checkpoints_dir: "checkpoints/adaln"
checkpoints_freq: 10
loss: null
optimizer:
  lr: 5e-4
  betas: [0.9, 0.999]
  weight_decay: 1e-4
scheduler:
  warmup_epochs: 4
early_stopping: null
data:
  data_dir: "data"
  train_bs: 32
  val_bs: 32
